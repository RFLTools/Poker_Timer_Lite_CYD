# Ignore binary files - these are built by GitHub Actions
*.bin

# These files will be generated during build
firmware.bin
bootloader.bin
partitions.bin
boot_app0.bin

# Keep the HTML and manifest
!index.html
!manifest.json
!README.md
